fastapi==0.115.0
uvicorn[standard]==0.30.6
motor==3.6.0
pymongo==4.9.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.2.0
aiosmtplib==3.0.2
# Bumped from 0.27.2 → 0.28.x to satisfy google-genai>=1.20.0 which
# requires httpx>=0.28.1. The httpx 0.27 → 0.28 jump is API-compatible
# for our usage (async client + basic request flow).
httpx>=0.28.1,<1.0
jinja2==3.1.4
python-multipart
# Pin a known-good floor — earlier 1.x builds reject
# ThinkingConfig(thinking_budget=...) with "Extra inputs are not
# permitted", which kills every SolverX Vertex AI call. Pip's
# resolver under `>=1.0.0` happily picks 1.2.0, so be explicit.
google-genai>=1.20.0,<2.0